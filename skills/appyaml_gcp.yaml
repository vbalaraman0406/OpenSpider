runtime: python311
entrypoint: gunicorn -w 4 -k uvicorn.workers.UvicornWorker backend.main:app --bind :$PORT
instance_class: F2
automatic_scaling:
  min_instances: 0
  max_instances: 3
env_variables:
  PYTHONPATH: /app
